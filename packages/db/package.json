{
    "name": "db",
    "version": "0.0.1",
    "scripts": {
        "generate": "npx prisma generate",
        "prisma:migrate": "npx prisma migrate dev --name init"
    },
    "dependencies": {
        "@prisma/client": "^4.11.0"
    }
}